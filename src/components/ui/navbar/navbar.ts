/**
 * Navbar component type definitions
 * Responsive: mobile = hamburger + logo (menu in drawer), desktop = logo + horizontal menu
 */

export interface NavbarProps {
  /** Optional breakpoint for responsive behavior (e.g. 'lg' = 1024px) */
  breakpoint?: string;
}
